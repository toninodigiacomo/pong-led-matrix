# Place here the games folder sources.

Install pre-requistes:
````bash
sudo apt update
sudo apt install python3-pygame python3-numpy
```

**SPACE INVADERS**
 ```bash
cd software/space-invaders
python3 main.py
```